import { LoginExistsValidatorDirective } from './login-exists-validator.directive';

describe('LoginExistsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginExistsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
